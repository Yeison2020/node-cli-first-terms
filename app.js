console.clear();

console.log("Working with CLI node first steps");

console.log("==================================");

const base = 5;
for (let i = 1; i <= 10; i++) {
  console.log(`${base} x ${i} = ${base * i}`);
}
